class SpotType {
  final int id;
  final String name;
  final int iconCode;

  SpotType({
    required this.id,
    required this.name,
    required this.iconCode,
  });

  // @override
  // bool operator == (dynamic other) => name == other!.name;

  // @override
  // int get hashCode => super.hashCode;
}
